# Evaluations

This folder is reserved for the study's future evaluation materials and derived
scores. It is intentionally empty apart from this note until the evaluation
protocol is finalized.

Keep all model generations in `../runs/results/`. Any future evaluation record
must reference the relevant generation `record_id` and must never overwrite or
edit a raw generation file.
